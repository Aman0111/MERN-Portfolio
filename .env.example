# Leave blank for local dev (Vite's proxy in vite.config.js handles it).
# Set this to your deployed server's URL only if the client and server are
# hosted on different domains, e.g.:
# VITE_API_URL=https://your-server.onrender.com
VITE_API_URL=
